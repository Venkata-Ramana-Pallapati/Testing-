apiVersion: v1
kind: Service
metadata:
  name: ramana-backend-service
  namespace: backend
spec:
  type: LoadBalancer
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
